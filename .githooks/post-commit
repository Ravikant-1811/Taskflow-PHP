#!/bin/sh
branch="$(git rev-parse --abbrev-ref HEAD 2>/dev/null)"
if [ -z "$branch" ] || [ "$branch" = "HEAD" ]; then
  exit 0
fi

git push >/dev/null 2>&1 || {
  echo "[post-commit] Auto-push failed. Run 'git push' manually." >&2
  exit 0
}

echo "[post-commit] Auto-pushed branch '$branch'."
